# Minimal dependencies for Magic26 CI probe / static package verification.
# Full refresh migration may need additional storage/cache work, but these cover
# current Python scripts imported by the probe and verifier.
pandas>=2.2,<3
numpy>=1.26,<3
requests>=2.31,<3
pyarrow>=16,<23
